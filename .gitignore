# Workshop runtime artifacts — never commit
workshop/keys/
workshop/nodes.csv
workshop/.env.workshop
workshop/attendees.csv
workshop/shard-assignments.csv
workshop/_*
# ^ was workshop/_*.sh, which missed the .log/.py/.awk scratch left by the
# May run. _finalize.log carries a provisioned instance's public IP.

# Claude Code session artifacts
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.ipynb_checkpoints/

# Recipe outputs (when this repo is cloned to run training)
runs/
models/
data/dolly_*.jsonl

# OS
.DS_Store
Thumbs.db

# Private corpus — real correspondence from other people. This repo is PUBLIC.
# The workshop scp's these onto the box, so they can appear in a clone.
data/*.jsonl
data/*.json
prompts/ray-*
*.reference.md
ray-sweep.tgz
!data/.gitkeep

# Unreferenced 2.9 MB binary, untracked since 6 May. Nothing in the repo links
# to it. Ignored rather than committed because publishing it to a public repo is
# a call for its author, not a side effect of a tree-clean check. Delete this
# line to ship it.
COMIC.pdf

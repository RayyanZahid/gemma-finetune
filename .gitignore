# Workshop runtime artifacts — never commit
workshop/keys/
workshop/nodes.csv
workshop/.env.workshop
workshop/attendees.csv
workshop/shard-assignments.csv
workshop/_*.sh
workshop/_*/

# Claude Code session artifacts
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.ipynb_checkpoints/

# Recipe outputs (when this repo is cloned to run training)
runs/
models/
data/dolly_*.jsonl

# OS
.DS_Store
Thumbs.db

# Private corpus — real correspondence from other people. This repo is PUBLIC.
# The workshop scp's these onto the box, so they can appear in a clone.
data/*.jsonl
data/*.json
prompts/ray-*
*.reference.md
ray-sweep.tgz
!data/.gitkeep
